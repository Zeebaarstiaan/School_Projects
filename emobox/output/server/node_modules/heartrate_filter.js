exports.filterData = function (data) {
    var filteredData = new Array();
    for (var index in data) {
        var heartrate = data[index];
        if (heartrate <= 200 && heartrate >= 49) {
            filteredData.push(heartrate);
        }
    }
    return filteredData;
};